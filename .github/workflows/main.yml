name: Keep Render alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl --silent --output /dev/null https://my-website-g7nn.onrender.com || true
          echo "Ping sent"
